import { ChildrenDirective } from './children.directive';

describe('ChildrenDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildrenDirective();
    expect(directive).toBeTruthy();
  });
});
